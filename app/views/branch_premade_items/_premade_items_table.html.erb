<ul class="nav nav-tabs">
  <li class="nav-item">
    <a class="nav-link" href="<%= branch_inventories_path %>" data-method = 'get' >Market Items</a>
  </li>
  <li class="nav-item">
    <a class="nav-link active" href="#!">Premade Items</a>
  </li>
</ul>

<table class = 'table'>
  
  <thead>
    <tr>
      <th>
        Items
        <%= link_to '[+]', branch_premade_items_path( list: true ), method: 'get' %>
      </th>
      
      <th>
        Stock
        <%= link_to pen_edit_unicode, edit_individual_branch_premade_items_path, method: 'post' %>
      </th>
    </tr>
  </thead>
  
  <% @branch_premade_items.each do | join | %>
    <% item = PremadeItem.find( join.premade_item_id ) %>
  
    <tbody>
      <tr>
        <td>
          <%= link_to item.name, edit_branch_premade_item_path( join ), method: 'get' %>
        </td>

        <td>
          <%= inventory_stock_of( join, true ) %>
        </td>
      </tr>
    </tbody>
  <% end %>
</table>
